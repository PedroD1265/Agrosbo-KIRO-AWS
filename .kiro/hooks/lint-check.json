{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint Check",
      "trigger": "PostFileSave",
      "description": "Runs ESLint on TypeScript files after save. Deterministic.",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx eslint --no-error-on-unmatched-pattern \"api/src/**/*.ts\" \"web/src/**/*.{ts,tsx}\" \"shared/**/*.ts\"",
        "timeout": 30
      }
    }
  ]
}
