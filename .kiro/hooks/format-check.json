{
  "version": "v1",
  "hooks": [
    {
      "name": "Format Check",
      "trigger": "PostFileSave",
      "description": "Checks Prettier formatting on TypeScript files after save. Deterministic.",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx prettier --check --ignore-unknown \"api/src/**/*.ts\" \"web/src/**/*.{ts,tsx}\" \"shared/**/*.ts\"",
        "timeout": 30
      }
    }
  ]
}
