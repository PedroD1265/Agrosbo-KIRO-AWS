# Spike A - Lab-only PostgreSQL credentials (synthetic, not sensitive)
# Copy to .env.spike for local use. The real file is gitignored.
POSTGRES_DB=agrosbo_spike
POSTGRES_USER=spike
POSTGRES_PASSWORD=spike_local_only
