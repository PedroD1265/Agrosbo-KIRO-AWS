name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Clean
        run: npm run clean

      - name: Format
        run: npm run format

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test (Unit)
        run: npm test

      - name: Test MemStorage HTTP
        run: npm run test:memstorage

      - name: Check UTF-8 integrity
        run: npm run check:encoding

      - name: Build
        run: npm run build

  integration-postgres:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: agrosbo_test
          POSTGRES_USER: agrosbo
          POSTGRES_PASSWORD: agrosbo_ci
        options: >-
          --health-cmd "pg_isready -U agrosbo"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      DATABASE_URL: postgres://agrosbo:agrosbo_ci@localhost:5432/agrosbo_test
      NODE_ENV: test
      AUTH_ENFORCEMENT: 'off'
      APP_AUTH_PROVIDER: local-session
      ATTACHMENTS_STORAGE_DRIVER: local
      DOCUMENT_EXTRACTION_PROVIDER: none
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run DB Migrations
        run: npm run db:migrate

      - name: Seed Database
        run: npm run db:seed

      - name: Run PostgreSQL Integration Tests
        run: npm run test:integration

      - name: Re-run DB Migrations (Idempotency Check)
        run: npm run db:migrate

      - name: Check DB Schema Consistency
        run: npm run db:check
